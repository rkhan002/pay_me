{
  "name": "@pay-me/rules-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Server-authoritative Pay Me game logic: dealing, melds, wild rules, scoring, turn state machine. Pure functions, framework-free, importable from Deno edge functions or Node tests.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
